# Features/DeckDetail

Maps to the Android package `dev.memoji.flashcards.feature.deckdetail`.

Empty for now — filled by the ticket that needs it (Deck detail). The folder exists so the mapping
between this app's folders and the Android app's packages is established up front rather than
negotiated per ticket.

This file is excluded from the app target via `membershipExceptions` in the project file, so it
is not copied into the app bundle. Delete it once the folder has real contents.
